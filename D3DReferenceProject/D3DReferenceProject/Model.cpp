#include "Model.h"

uint32_t modelCtr = 0;

Model::Model(const char* path)
{
	// Load mesh into buffers with tinyobjloader or directxmesh (not sure which to use yet)

	// Mesh material IDs are always diffuse for now, model IDs are generated with [modelCtr]
}
